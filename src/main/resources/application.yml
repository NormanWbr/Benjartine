server:
  port: 5432

spring:

  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/benjartine_db
    password: eqc116
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  banner:
    location: banner.txt
    